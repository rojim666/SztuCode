# 协议兼容说明

办公 RPC 采用新增方法方式，不改变既有 JSON-RPC 2.0/NDJSON 请求和事件字段语义：

- `artifact.*`：成果查询、注册、验证
- `operation.*`：操作查询、恢复核对
- `schedule.*`：定时任务 CRUD

旧客户端可继续使用旧方法；新客户端应通过 daemon capability/方法错误判断后端是否支持办公能力。未知返回字段应忽略；不支持的方法返回标准 `-32601`。发送/发布仍与草稿创建分离。
