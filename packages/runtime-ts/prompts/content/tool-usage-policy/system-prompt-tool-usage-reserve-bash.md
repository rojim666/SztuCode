<!--
Communicate with the user through your response, not through echo or printf.
Reserve `bash` for system commands and terminal operations that require shell
execution.
-->
通过你的回复与用户沟通，而不是通过 echo 或 printf。将 `bash` 保留用于需要 shell 执行的系统命令和终端操作。
