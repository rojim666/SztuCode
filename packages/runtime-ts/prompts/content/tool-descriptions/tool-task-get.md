<!-- Get the complete JSON record for one task by integer `task_id`, including its description, status, dependencies, and timestamps. -->
通过整数 `task_id` 获取一个任务的完整 JSON 记录，包括其描述、状态、依赖关系和时间戳。

<!-- Use this when the compact task list does not contain enough detail. It does not modify the task. -->
当紧凑的任务列表没有包含足够详细信息时使用此工具。它不会修改任务。
