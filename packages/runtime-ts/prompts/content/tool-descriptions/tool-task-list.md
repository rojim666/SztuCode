<!-- List all tasks for the current run with their status and blocking dependencies. -->
列出当前运行的所有任务及其状态和阻塞依赖关系。

<!-- Use this to review remaining work, identify which pending tasks are unblocked, and keep a multi-step plan synchronized. It does not modify tasks. -->
用于审查剩余工作，识别哪些待处理任务已解除阻塞，并保持多步计划同步。它不会修改任务。
