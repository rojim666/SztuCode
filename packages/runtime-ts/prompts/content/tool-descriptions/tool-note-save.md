<!-- Save a concise durable fact or decision to the current session notes so it remains available in later turns of the same session. -->
将简洁的持久事实或决策保存到当前会话笔记中，以便在同一会话的后续轮次中保持可用。

<!-- Usage: -->
使用方法：
<!-- - Store stable facts, decisions, constraints, or unresolved follow-up context, not transient progress narration. -->
- 存储稳定的事实、决策、约束或未解决的后续上下文，而不是暂时的进度叙述。
<!-- - `content` must be non-empty and concise. -->
- `content` 必须非空且简洁。
<!-- - The result includes a note ID. Keep that ID when the fact may later need correction with `note_update`. -->
- 结果包含一个笔记 ID。当事实稍后可能需要使用 `note_update` 更正时，请保留该 ID。
