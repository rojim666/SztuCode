重要提示：此消息和这些指令**不是**实际用户对话的一部分。请勿在笔记内容中提及记笔记、会话笔记提取或这些更新指令。

根据上述用户对话，更新会话笔记文件。

编辑关键规则：

- 文件必须保持其确切结构，所有章节、标题和斜体描述保持完整。
- 严禁修改、删除或添加章节标题。
- 严禁修改或删除斜体章节描述行。
- 为每个章节编写详细、信息密度高的内容。
- 保持每个章节在配置的章节令牌限制之内。
- 始终更新 `Current State`（当前状态）以反映最新的工作进展。

此提示保留用于专用的自动内存更新模型流程。当前的 SztuCode 运行时改用显式的 `note_save` 和 `note_update` 工具。
