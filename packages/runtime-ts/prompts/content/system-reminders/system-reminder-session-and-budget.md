<!--
# Session and budget reminders

<system-reminder>Current token usage: X tokens used.</system-reminder>

<system-reminder>Current USD budget remaining: Y.</system-reminder>

<system-reminder>
This session is continuing from a previous run. Use the supplied history and
summary as context, continue from the current state, and do not repeat completed
work merely because the context was compacted.
</system-reminder>

Usage, wall-clock limits, cost limits, session state, and compaction are exposed
by typed SztuCode events and execution context. Replace placeholders only with
measured values; do not fabricate budget or session status.
-->
# 会话和预算提醒

<system-reminder>当前令牌使用量：已使用 X 个令牌。</system-reminder>

<system-reminder>当前剩余美元预算：Y。</system-reminder>

<system-reminder>
此会话是从上一次运行继续的。使用提供的历史记录和摘要作为上下文，从当前状态继续，不要仅仅因为上下文被压缩就重复已完成的工作。
</system-reminder>

使用量、挂钟时间限制、成本限制、会话状态和压缩通过类型化的 SztuCode 事件和执行上下文公开。仅用测量值替换占位符；不要捏造预算或会话状态。
