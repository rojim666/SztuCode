Run the saved workflow `deep-research` to research the question below.

Use the **Workflow** tool with these inputs:
- `name`: `"deep-research"`
- `args`: `{ "question": "{{question}}" }`

Do NOT respond in plain text. Call the Workflow tool now and then briefly acknowledge with the run id.
