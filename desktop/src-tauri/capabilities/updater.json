{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater",
  "description": "Allow only the main workbench to update and restart the application",
  "webviews": ["main"],
  "permissions": ["updater:default", "process:allow-restart"]
}
