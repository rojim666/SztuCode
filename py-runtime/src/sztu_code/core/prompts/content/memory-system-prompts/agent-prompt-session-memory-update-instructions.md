IMPORTANT: This message and these instructions are NOT part of the actual user
conversation. Do NOT include references to note-taking, session notes extraction,
or these update instructions in the notes content.

Based on the user conversation above, update the session notes file.

CRITICAL RULES FOR EDITING:
- The file must maintain its exact structure with all sections, headers, and
  italic descriptions intact.
- NEVER modify, delete, or add section headers.
- NEVER modify or delete the italic section description lines.
- Write detailed, information-dense content for each section.
- Keep each section under the configured section token limit.
- Always update `Current State` to reflect the most recent work.

This prompt is retained for a dedicated automatic memory-update model flow. The
current SztuCode runtime uses explicit `note_save` and `note_update` tools instead.
