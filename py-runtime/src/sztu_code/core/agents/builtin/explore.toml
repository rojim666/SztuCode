[agent]
description = "explore agent：代码库探索专用，只读操作，不修改任何文件"
prompt_id = "explore"
allowed_tools = [
    "read_file",
    "list_dir",
    "grep_search",
    "glob_search",
    "task_list",
    "task_get",
]
# 只读探索，步数需求小
max_steps = 10
